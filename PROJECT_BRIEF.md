# NAVIR 项目说明（给另一台电脑上的 Codex）

更新：2026-09-23。请先读本文，再读 `HANDOFF.md` 的最新记录。本文解释目标和交付标准；`HANDOFF.md` 记录最近做了什么。两者都不能代替实际打开图片、PSD 和询问用户确认。

## 一句话目标

为虚构高速吹风机品牌 **NAVIR S1** 制作可放进设计师作品集的品牌与电商视觉作品，重点是有设计水准的画面和**真正可编辑的源文件**。这不是要开发购物网站、购物车、支付、后台或上线商品。

称呼用户为“燃燃様々”。既定品牌名 `NAVIR`、概念 `FLOW`、口号 `Shape the Air.` 不要重新命名。产品造型以 `assets/NAVIR_S1_Three_View_v1.png` 等三视图为准：圆环风口、后部进风、两枚圆形按键、手柄比例、底部格栅和线缆不要随意重设计。

## 当前真正要做的设计

主线是 **NAVIR S1 新品上市的竖版、连续 7 屏详情页作品集**，不是老品促销。画布计划为每屏 `1464 × 3416 px`，7 屏总长 `1464 × 23912 px`。页面要从“认识新品”走到“性能、结构、护理、使用场景、操作细节、品牌收尾”，每屏有独立职责，同时连成一套。

| 屏 | 职责 | 当前证据 |
| --- | --- | --- |
| A01 | 新品身份；完整产品与放大的品名 | 有 PNG 和可编辑 PSD 视觉样稿 |
| A02 | 性能数字；`110,000 RPM` 为主，`20 m/s`、`50 次/秒` 为辅 | 有 PNG 和可编辑 PSD 视觉样稿 |
| A03 | 可见气流与产品结构 | 仅有色块/排版规划；未核实成品 |
| A04 | 温控护理 | 仅有色块/排版规划；未核实成品 |
| A05 | 居家洗发后快速干发场景 | 仅有色块/排版规划；未核实成品 |
| A06 | 按键、进风口、握持等操作细节 | 仅有色块/排版规划；未核实成品 |
| A07 | 安静、完整的品牌收尾 | 用户记得曾看过末页，但仓库里尚未辨认出获确认的最终 PNG/PSD；先核对，不要重做或宣称完成 |

这一阶段的色彩以 `portfolio/Amazon_APlus/Visual_Sample_v01/` 中的**蓝、深蓝、冷白、冷灰**视觉样稿为参照；产品本身保持珍珠白、钛银、石墨黑，不要被蓝色染色。`110,000 RPM` 等是作品集概念参数，不是实测或商用认证。避免未经确认的新数字、内部马达结构、折扣价格、评论、购物按钮、密集孔洞/蜂窝纹理和泛滥的装饰线。

`portfolio/Amazon_APlus/Layout_Approval/NAVIR_S1_Vertical_BlockLayout_v04_CN.psd` 是先行确认的**色块排版稿**，不是七屏完成设计。`Visual_Sample_v01/` 的 A01/A02 是下一确认节点的视觉样稿；文件已存在并可编辑，但不要把“文件存在”自动等同于用户已批准整套视觉。

## “源文件”在这里是什么意思

用户要的是能继续在 Photoshop 里改的 `.psd`，不仅是漂亮的 PNG/JPG。交付新页面时应同时提供：

- 分层 PSD：中文/英文文案保持原生可编辑文字；产品尽可能作为可替换的智能对象；背景、色块、阴影、气流、场景等分组独立。不要把整张 PNG 塞进 PSD 伪装成源文件。
- 对应的 PNG/JPG 预览，方便用户在 Codex 对话和普通图片查看器中直接看画面。
- 必要时提供拼接长图供检查连续节奏；保留原来的版本，不覆盖已交付文件。

已有可编辑实例：

- `portfolio/Amazon_APlus/Visual_Sample_v01/NAVIR_S1_A01_Hero_CN_v01.psd` + 同名 PNG
- `portfolio/Amazon_APlus/Visual_Sample_v01/NAVIR_S1_A02_Performance_CN_v01.psd` + 同名 PNG
- `portfolio/Amazon_APlus/Layout_Approval/NAVIR_S1_Vertical_BlockLayout_v04_CN.psd` + 同名 PNG（这是排版稿，不是最终视觉）

`build_navir_visual_sample_v01.jsx` 是制作脚本，`tmp/` 是 QA/参考图，二者都**不能代替**可编辑 PSD 与可观看的导出图。检验源文件时要实际检查图层、文字和产品对象；只看文件扩展名不够。

## 仓库里的其他东西，别混为一谈

- `portfolio/Amazon_APlus/Source/`、`Exports/`、`Review_v01/` 等含**更早的横版 A+ 探索**，尺寸和语言与当前竖版主线不同。保留作历史与素材，不要直接拿来宣布七屏完成。
- `deliverables/NAVIR_PC_Template_GreyWhite_A_Airy.png` 是另一个 **PC 整页灰白结构草图**。用户在家里选择了 A 作为该结构的出发点；它不是最终品牌视觉，也不等于上面的竖版七屏。B/C 是比较稿。
- `navir-visual-philosophy*.md`、品牌板和早期 KV 中的暖白/酒红属于较早的品牌探索。不要把那套酒红规则强加到已经采用蓝色的 NAVIR S1 竖版视觉样稿；若要重新统一两个方向，先给用户看清差异并取得选择。

## 接下来如何接手

1. 在家用电脑的 NAVIR 仓库先 `git pull --ff-only`，确认 `main` 与 `origin/main` 一致；有本地修改时不要强行覆盖。
2. 直接向用户展示 A01、A02、竖版 v04 色块稿及 PC A 结构图，明确标注“视觉样稿 / 排版稿 / PC 结构稿”。不要只报文件名。
3. 和用户核对记忆中的“第一页、最后一页”究竟是哪张；尤其 A07 最终稿在仓库里尚未确认。只问缺失的关键选择，不重新开始品牌讨论。
4. 确认要继续**竖版七屏**还是另做 **PC 整页**后再制作。若继续竖版，应在已有 A01/A02 基础上推进下一屏，并同时交付可编辑 PSD 与预览图；不要只写脚本、网页代码或文字方案。
5. 每次结束前更新 `HANDOFF.md`，说明新增源文件/预览、验证结果、尚待用户确认的问题；提交并推送，另一台电脑才能接上。

历史文件可能有不同规格与颜色。遇到冲突时，按具体工作流和较新的用户确认区分，不能静默覆盖或混用。
